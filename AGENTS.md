# Repository Instructions

## Completing Changes

- After modifying code or repository files, commit the completed changes and push the current branch.
- Treat committing and pushing as one continuous completion step unless the user explicitly asks not to commit or not to push.
- Stage only files related to the requested change; exclude generated artifacts and unrelated local files.
